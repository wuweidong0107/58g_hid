- 将 usb_hid_write 放入线程池中 (OK);
- 设备有 client 列表 (OK);
- 增加非交互模式 (OK);
- 增加安静模式 (OK);
- 支持控制 wifi (WIP);
- usb 支持 vid/pid 指定(WIP)
- rktools / io (WIP)
- gpio;
- input;
- bt;
- pwm;
- Thermal;
- cpufreq;
- mmio;
- led;
- udev 检测提取为公共组件;
- 多个模块可共用同一个串口，例如在 /dev/ttyS1 上，既要解析MCU 控台数据，又要解析 DSP 数据。
